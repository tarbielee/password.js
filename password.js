"use strict"
function passwordIsValid(password) {

    let counter = 0

    var myRegex = newRegExp
    ((B[A-2])(B[9-2])(B[/d])(B[@*$]){8,})
    if (password.match(myRegex)) {
        return("password valid");
    }
    else
    {
        return("password invalid");
    }
} 
passwordIsValid ("Hello")
passwordIsValid ("hello_universe")
passwordIsValid ("Hello_123")
    try{
        if(password.Match(l[A-2]/g))=num);
        if(password.Match(l[a-2]/g))=num);
    }
    catch(password) {
        return error


    }
}